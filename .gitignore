node_modules/
.dev.vars
# Root-level log files only. A bare `log*` also matches anything beginning with
# "log" at any depth, which silently swallowed assets/brand/logo.svg.
/log*
_tests/
.wrangler

# make_outlines.py imports spec.py, so Python leaves bytecode here
__pycache__/
